import Layout from "../../../layout";
import Requests from "../../../components/userAccount/requests";
const RequestPage = () => {
  return (
    <Layout>
      <Requests />
    </Layout>
  );
};

export default RequestPage;
