import Layout from "../../../layout";
import Trip from "../../../components/receivingParcel/trip";
const TripPage = () => {
  return (
    <Layout>
      <Trip />
    </Layout>
  );
};

export default TripPage;
